@inherits Umbraco.Web.Mvc.UmbracoViewPage<Umbraco.Web.PublishedModels.Advertisements>
@{
    Layout = "Master.cshtml";
}

@* the fun starts here *@



@* the fun starts here *@

<main id="main">
    @*<section id="topsection" class="d-flex align-items-center">
            <div class="container">
                <h1></h1>
                <h2>@Model.Name</h2>
            </div>
        </section>*@
    <section id="about" class="about mt-5">
        <div class="container-fluid mt-5">

            <div class="container-fluid">
                <div class="row">
                    @Html.Partial("~/Views/Partials/sideMenu.cshtml")
                    <div class="col-xl-7 col-lg-7 icon-boxes d-flex flex-column align-items-stretch ">
                        <img src="~/assets/img/015-telegram.svg" class="bg-image" />
                        <div class="section-title">
                            <h2>
                                @if (!string.IsNullOrWhiteSpace(Model.HeaderTitle))
                                {
                                    <span>@Model.HeaderTitle</span>
                                }
                                else
                                {
                                    <span>@Model.Name</span>
                                }
                            </h2>
                            @if (!string.IsNullOrWhiteSpace(Model.HeaderSubtitle))
                            {
                                <span>@Model.HeaderSubtitle</span>
                            }
                        </div>
                        @Html.Partial("~/Views/Partials/latestAdvertisements.cshtml")
                    </div>
                </div>
            </div>
        </div>
    </section>

</main><!-- End #main -->
